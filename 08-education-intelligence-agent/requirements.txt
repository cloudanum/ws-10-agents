# ===========================================================================
# requirements.txt — Workshop Agent 8: The Education Intelligence Agent
# Book: 30 Agents Every AI Engineer Must Build (Packt Publishing)
# Author: Imran Ahmad
#
# Install: pip install -r requirements.txt
# Python: >=3.10, <3.13
# ===========================================================================

# === Core Dependencies (Ch.15, p. 2) ===
numpy==1.26.4
networkx==3.3
python-dotenv==1.0.1

# === Live Mode — OpenAI provider (used automatically when OPENAI_API_KEY is
# set in .env; without a key the notebook runs in Simulation Mode and never imports it) ===
openai>=1.40.0,<2.0.0

# === Jupyter Runtime ===
jupyter>=1.0.0
ipykernel>=6.29.0
notebook>=7.0.0

# === Standard Library (no install — listed for reference) ===
# math, dataclasses, datetime, functools, typing, getpass, re, json
